{
  "name": "skriblerne-api",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node api/index.js",
    "vercel-build": "mkdir -p .vercel/output && cp -r api .vercel/output/"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  }
}
